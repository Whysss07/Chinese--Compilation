# 顺序结构
令 数字 = 0
输出("程序开始")

# 分支结构
如果（数字 == 0）
{
    输出("数字当前等于 0")
}
否则如果（数字 == 1）
{
    输出("数字当前等于 1")
}
否则
{
    输出("数字不是 0，也不是 1")
}

# 计数循环
重复（3）
{
    输出("这是重复循环")
}

# 条件循环
当（数字 < 3）
{
    输出("当前数字是", 数字)
    数字 = 数字 + 1
}

输出("程序结束，最终数字 =", 数字)
